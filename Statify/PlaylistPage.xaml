<Page x:Class="Statify.PlaylistPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Statify"
      mc:Ignorable="d"
      d:DesignHeight="900" d:DesignWidth="1600"
      Title="PlaylistPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListView Grid.Column="0" x:Name="ListViewPlaylists" ItemsSource="{Binding TopPlaylists}" SelectionChanged="Selector_OnSelectionChanged" FontSize="22" Margin="20"/>
        <ListView Grid.Column="1" ItemsSource="{Binding Tracks}" FontSize="22" Margin="20"/>
    </Grid>

</Page>