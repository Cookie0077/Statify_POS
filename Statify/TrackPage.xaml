<Page x:Class="Statify.TrackPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Statify"
      xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TrackPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <ListView Grid.Column="0" x:Name="ListViewTracks"  ItemsSource="{Binding TopTracks}"></ListView>
        <lvc:CartesianChart Grid.Column="1" Background="MediumAquamarine" Series="{Binding TrackSeries}" Width="250" Height="300"/>
        <DataGrid Name="TrackGrid" 
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  Background="Gray"
                  RowBackground="LightCyan"
                  AlternatingRowBackground="AntiqueWhite">
            
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Cover" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding Image}" Width="40" Height="40" Margin="5"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Track Name" Binding="{Binding Name}" Width="2*" FontWeight="Bold"/>

                <DataGridTextColumn Header="Artist" Binding="{Binding Artist}" Width="1.5*"/>

                <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="0.8*"/>

                <DataGridTextColumn Header="Plays" Binding="{Binding PlayCount}" Width="0.5*"/>

                <DataGridTextColumn Header="Last Played" Binding="{Binding LastPlayed, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" Width="1.5*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
