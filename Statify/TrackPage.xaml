<Page x:Class="Statify.TrackPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Statify"
      xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
      mc:Ignorable="d"
      d:DesignHeight="900" d:DesignWidth="1600"
      Title="TrackPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListView Grid.Column="0" x:Name="ListViewTracks" ItemsSource="{Binding TopTracks}" FontSize="22" Margin="20">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Track Name" DisplayMemberBinding="{Binding Name}" Width="300"/>
                    <GridViewColumn Header="Artist" DisplayMemberBinding="{Binding Artist}" Width="220"/>   
                    <GridViewColumn Header="Duration" DisplayMemberBinding="{Binding Duration}" Width="160"/>
                    <GridViewColumn Header="Plays" DisplayMemberBinding="{Binding PlayCount}" Width="140"/>
                    <GridViewColumn Header="Last Played" DisplayMemberBinding="{Binding LastPlayed, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" Width="260"/>
                </GridView>
            </ListView.View>
        </ListView>

        <lvc:CartesianChart Grid.Column="1" x:Name="TrackChart" Background="MediumAquamarine" Margin="20"/>
    </Grid>

</Page>