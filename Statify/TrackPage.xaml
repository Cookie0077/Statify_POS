<Page x:Class="Statify.TrackPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Statify"
      xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
      mc:Ignorable="d"
      d:DesignHeight="900" d:DesignWidth="1600"
      Title="TrackPage"
      Background="Transparent">

    <Grid Margin="32 24 32 24">
        <Grid x:Name="ContentGrid" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="6 0 6 18">
                <TextBlock Text="LIBRARY" Style="{StaticResource EyebrowTextStyle}" />
                <TextBlock Text="Songs" Style="{StaticResource HeadlineTextStyle}" Margin="0 4 0 4" />
                <TextBlock Text="Every track you've played, ranked and ready."
                           Style="{StaticResource MutedTextStyle}" />
            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top"
                                   Text="All Tracks"
                                   Style="{StaticResource SectionTitleStyle}"
                                   Margin="0 0 0 12" />
                        <ListView x:Name="ListViewTracks"
                                  ItemsSource="{Binding TopTracks}"
                                  FontSize="15"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  MouseDoubleClick="ListViewTracks_OnMouseDoubleClick">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Image">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <Image Source="{Binding Image}" MaxHeight="48"></Image>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>

                                    <GridViewColumn Header="Track Name" DisplayMemberBinding="{Binding Name}"
                                                    Width="300" />
                                    <GridViewColumn Header="Artist" DisplayMemberBinding="{Binding Artist}" Width="220" />
                                    <GridViewColumn Header="Duration"
                                                    DisplayMemberBinding="{Binding Duration, Converter={StaticResource MsToDuration}}"
                                                    Width="100" />
                                    <GridViewColumn Header="Plays" DisplayMemberBinding="{Binding PlayCount}"
                                                    Width="65" />
                                    <GridViewColumn Header="Last Played"
                                                    DisplayMemberBinding="{Binding LastPlayed, StringFormat=\{0:yyyy-MM-dd HH:mm\}}"
                                                    Width="200" />
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Top" Margin="0 0 0 12">
                            <TextBlock Text="TRENDS" Style="{StaticResource EyebrowTextStyle}" />
                            <TextBlock Text="Play Counts" Style="{StaticResource SectionTitleStyle}" Margin="0 4 0 0" />
                        </StackPanel>
                        <lvc:PieChart x:Name="TrackChart"
                                      Background="Transparent"
                                      Margin="0 8 0 0"
                                      MinHeight="360" />
                    </DockPanel>
                </Border>
            </Grid>
        </Grid>
        <local:LoadingOverlay x:Name="LoadingOverlay" />
    </Grid>
</Page>