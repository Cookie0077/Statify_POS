<Page x:Class="Statify.TrackPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Statify"
      xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TrackPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <ListView Grid.Column="0" x:Name="ListViewTracks"  ItemsSource="{Binding TopTracks}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Track Name" DisplayMemberBinding="{Binding Name}"></GridViewColumn>
                    <GridViewColumn Header="Artist" DisplayMemberBinding="{Binding Artist}"></GridViewColumn>
                    <GridViewColumn Header="Duration" DisplayMemberBinding="{Binding Duration}"></GridViewColumn>
                    <GridViewColumn Header="Plays" DisplayMemberBinding="{Binding PlayCount}"></GridViewColumn>
                    <GridViewColumn Header="Last Played" DisplayMemberBinding="{Binding LastPlayed, StringFormat=\{0:yyyy-MM-dd HH:mm\}}"></GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <lvc:CartesianChart Grid.Column="1" x:Name="TrackChart" Background="MediumAquamarine" Series="{Binding TrackSeries}" Width="250" Height="300"/>
        
    </Grid>
</Page>
