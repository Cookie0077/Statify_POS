<UserControl x:Class="Statify.SpotfyItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Statify"
             mc:Ignorable="d" d:DesignHeight="550" d:DesignWidth="200">
    <Grid  Background="">

        <Grid.RowDefinitions>
            
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" ></ColumnDefinition>
            <ColumnDefinition Width="4*" ></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ListView Grid.Column="0" HorizontalAlignment="Right" Padding="10" ItemsSource="{Binding Track_Images}" Margin="0 50" ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
        <ListView Grid.Column="1" Margin="0 50 5 50" MinWidth="150" MaxWidth="220" Padding="10" HorizontalAlignment="Left" ItemsSource="{Binding TopTracks}" />
    </Grid>
</UserControl>
