<UserControl x:Class="Statify.SpotfyItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Statify"
             mc:Ignorable="d" d:DesignHeight="900" d:DesignWidth="600">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="800"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <ListView x:Name="ListViewImages" Grid.Column="0" HorizontalAlignment="Right" Padding="20" ItemsSource="{Binding Item_Images}" Width="100" FontSize="18" ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
        <ListView x:Name="ListviewItems" Grid.Column="1" Width="500" Padding="20" FontSize="22" HorizontalAlignment="Left" ItemsSource="{Binding SpotifyItems}" SelectionChanged="Selector_OnSelectionChanged"/>
    </Grid>

</UserControl>