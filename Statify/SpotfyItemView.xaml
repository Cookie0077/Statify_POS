<UserControl x:Class="Statify.SpotfyItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Statify"
             mc:Ignorable="d" d:DesignHeight="550" d:DesignWidth="200">
    <Grid  Background="Transparent">

        <Grid.RowDefinitions>
            <RowDefinition Height="500"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" ></ColumnDefinition>
            <ColumnDefinition Width="4*" ></ColumnDefinition>
        </Grid.ColumnDefinitions>


        <ListView x:Name="ListViewImages" Grid.Column="0" HorizontalAlignment="Right" Padding="10" ItemsSource="{Binding Item_Images}" Width="50"  ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
        <ListView Grid.Column="1" Width="300" Padding="10" HorizontalAlignment="Left" ItemsSource="{Binding SpotifyItems}" />
    </Grid>
</UserControl>
